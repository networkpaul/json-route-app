services:
  flask-app:
    image: flask-json-ui
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - data:/app
      - routes:/app/routes
    environment:
      - FLASK_APP=app.py

volumes:
  data:
  routes:
